%h1

%table
  %thead
    %tr
      %th Person type
      %th Person
      %th Amount
      %th Start date
      %th End date
      %th Scholarship type
      %th Status
      %th
      %th
      %th

  %tbody
    - @scholarships.each do |scholarship|
      %tr
        %td= scholarship.person_type
        %td= scholarship.person_id
        %td= scholarship.amount
        %td= scholarship.start_date
        %td= scholarship.end_date
        %td= scholarship.scholarship_type
        %td= scholarship.status
        %td= link_to 'Show', scholarship
        %td= link_to 'Edit', edit_scholarship_path(scholarship)
        %td= link_to 'Destroy', scholarship, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Scholarship', new_scholarship_path
