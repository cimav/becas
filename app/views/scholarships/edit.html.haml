%nav.config-breadcrumb.z-depth-0
  .nav-wrapper
    .row
      .col.s12.m12.l12
        =link_to root_path, class:'breadcrumb' do
          Listado de becas
        %a.breadcrumb{}
          Editar beca

%h1 Editar beca

-if is_admin?
  = render 'form'
-else
  =render 'staff_scholarship_form'